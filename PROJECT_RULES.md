# HanClip 프로젝트 개발 규칙

이 문서는 HanClip을 개발하면서 사람과 Codex가 공통으로 지켜야 할 규칙을 정리한다.

## 공동부분
- 코덱스의 사용에 있어 불필요하고 반복적인 행위를 삼가하여 주간사용량과 크레딧 사용량을 줄이려 노력한다.

## 1. 작업 시작과 범위

- 작업을 시작하기 전에 짧은 작업 계획을 먼저 작성해 보여준다.
- HanClip 작업 중에는 이 프로젝트 경로 밖의 파일을 수정하거나 삭제하지 않는다.
- 사용자가 만든 변경사항과 현재 작업과 관계없는 변경사항은 임의로 되돌리거나 덮어쓰지 않는다.
- 요구사항이 불명확하면 기존 동작과 사용자 데이터를 보존하는 방향을 우선한다.
- 야간처럼 장시간 자율 작업을 진행할 때 중간 진행률 보고는 핵심 완료 항목과 사용량만 간략히 쓰고, 상세 변경 내역은 최종 보고에 모아 정리한다.

## 2. 소스 수정과 검증

- 소스를 수정하면 먼저 컴파일 오류를 확인하고, 오류가 있으면 다른 작업보다 우선해서 해결한다.
- 소스 수정 후에는 빌드하고 시뮬레이터에 설치한 뒤 앱을 실행해 확인한다.
- 설치 10회마다 한 번은 기존 앱을 삭제한 후 새로 설치해 초기 설치 상태도 검증한다.
- 실기기 기능, 카메라, 위젯, 공유 확장처럼 시뮬레이터만으로 확인할 수 없는 기능은 가능한 경우 아이폰에서도 검증한다.
- 화면 수정은 앱이 실행되는지만 보지 않고 버튼의 표시, 터치 범위, 상태 전환까지 확인한다.
- 문서만 수정한 경우에는 앱 빌드와 설치를 생략할 수 있다.

## 3. 버전 관리

- 앱 버전은 별도 변경 지시가 있을 때까지 `1.0.1`로 유지한다.
- 빌드 번호는 기존 번호에서 이어서 관리하며, 빌드할 때마다 올린다.
- 소규모 수정은 빌드 번호를 `0.0.1`, 큰 수정은 `0.1.0` 단위로 올린다.
- 수정 규모의 판단은 변경 범위와 사용자 영향도를 기준으로 Codex가 한다.
- Ai 버전은 앱 버전 및 빌드 번호와 별도로 관리한다.

## 4. 커밋과 푸시

- 소스 수정 횟수를 누적해서 세고, 10회가 되면 지금까지 진행한 내용을 간단히 정리해 커밋하고 푸시한다.
- 큰 수정을 완료하면 10회를 기다리지 않고 변경사항을 스테이징한 뒤 내용을 간단히 정리해 커밋하고 푸시한다.
- 커밋에는 현재 작업과 관련된 파일만 포함하고, 사용자 개인 설정이나 빌드 산출물은 포함하지 않는다.
- 푸시 전에 현재 브랜치, 원격 변경사항, 충돌 가능성을 확인한다.
- 충돌이 발생하면 HanClip의 현재 작업 소스를 우선해 해결하고, 충돌 내용과 처리 결과를 사용자에게 알린다.
- 강제 푸시가 필요한 경우 원격 변경 내용을 먼저 확인하고 HanClip 작업 내용이 유실되지 않도록 한다.

## 5. 협업과 재현 가능한 빌드

- 다른 개발자가 `git clone`한 뒤 별도의 로컬 수정 없이 Xcode에서 빌드할 수 있어야 한다.
- 빌드에 필요한 프로젝트 설정, 권한, 리소스, 폰트 및 Target Membership 변경은 저장소에 포함한다.
- 개인 Mac에만 존재하는 절대 경로, 사용자별 Xcode 설정, DerivedData에 의존하지 않는다.
- 원격 브랜치를 병합하기 전 변경 범위와 충돌 여부를 확인하고, 충돌이 있으면 사용자에게 알린다.

## 6. Android 개발 추가 규칙

- Android 개발에도 소스 수정 10회마다 진행 내용을 간단히 정리해 커밋하고 푸시하는 규칙을 동일하게 적용한다.
- Android 개발 중에는 변경된 iOS 기능을 하루에 한 번 확인해 플랫폼 차이를 점검한다.
- iOS 비교에서는 기능과 수치뿐 아니라 모든 화면의 텍스트, 항목별 아이콘, 버튼 동작, 페이지 존재 여부, 배치·간격·크기·색상·상태 디자인을 함께 대조한다.
- iOS의 텍스트·아이콘·핵심 동작은 기준으로 정확히 맞추고, Android의 넓은 화면·성능·접근성처럼 더 잘할 수 있는 부분은 한 단계 개선한다. 단, 개선을 이유로 기능의 의미를 iOS와 다르게 바꾸지 않는다.
- iOS에 있고 Android에 없는 항목은 iOS 구현이 완료된 뒤 Android에 이식한다. 바로 이식할 수 없는 항목은 누락시키지 않고 항목명, 차이, 미구현 이유, 필요한 후속 작업을 동등성 문서에 기록해 완료 보고에 포함한다.
- iOS에서 작업 중인 기능을 Android에서 먼저 추측해 동시에 만들지 않는다. iOS 구현이 안정된 뒤 다음 일일 점검에서 기준을 가져와 한 번에 반영한다.
- Android 화면의 카드, 버튼, 입력창, 팝업, 플로팅 패널은 iOS 시각 언어에 맞춰 둥근 사각형을 기본으로 한다. 전체 화면 루트, 사진·영상 원본, 달력 격자와 구분선처럼 기능상 사각형이어야 하는 요소만 예외로 둔다.
- 앱 안에 떠오르는 패널은 투명 배경 때문에 내용이 흐려지지 않도록 테마별 불투명 패널색, 16dp 이상의 둥근 모서리, 얇은 테두리와 절제된 그림자를 기본으로 사용한다.
- Mac과 휴대폰이 `zangzip` 네트워크에 연결되어 있으면 무선 디버깅을 우선 사용하고, 연결할 수 없을 때만 USB를 사용한다.
- Android 실기기 작업을 시작하면 화면 자동 꺼짐을 해제하고 충전 중 항상 켜짐을 활성화해 작업 도중 화면이 잠기지 않게 유지한다. 이미 잠긴 상태라면 자격 증명을 추측하거나 입력하지 않고 사용자가 한 번 직접 잠금을 해제하도록 안내한다.
- 사용자가 같은 화면 제목의 참고 사진을 다시 주면 질문 없이 최신 상태 또는 펼침/접힘 등 추가 상태로 판단해 Android 디자인 기준에 반영한다.
- Android 화면은 바형 휴대폰을 기본 기준으로 하되 화면 폭 600dp 이상에서는 단순 확대하지 않고 프리셋·컬렉션의 열 수를 늘리거나 편집·미리보기 영역을 좌우로 나눠 태블릿, 트라이폴드, Fold 펼침 화면의 넓이를 작업 효율에 활용한다. 접힘·펼침과 회전 중에도 선택 상태와 편집 내용을 보존한다.

## 7. 완료 보고

- 작업 완료 시 수정한 핵심 내용, 빌드 결과, 설치 및 실행 결과를 간단히 보고한다.
- 커밋이나 푸시를 하지 않았다면 그 상태도 명확히 알린다.
- 앱 삭제, 데이터 초기화, 강제 푸시처럼 복구가 어려울 수 있는 작업은 실행 결과와 복구 가능 여부를 알린다.

## 8. 카피라이터 기능 사전 관리

- 새 기능을 만들거나 기존 기능의 명칭·핵심 동작을 변경하면 앱의 카피라이터 정보창에 있는 기능 사전에도 같은 작업에서 반영한다.
- 기능 사전에는 사용자가 화면에서 보는 명칭, 기능의 목적, 주요 선택지와 중요한 동작 기준을 짧고 명확하게 기록한다.
- 기능이 없어지거나 명칭이 바뀌면 오래된 설명을 방치하지 않고 함께 수정하거나 제거한다.

## 9. `N%로 작업 시작해` 단축 지시

- 사용자가 `N%로 작업 시작해`라고 말하면 `N`을 이번 작업에 허용한 주간 사용량 퍼센트포인트로 해석한다. 예를 들어 `1%로 작업 시작해`, `3%로 작업 시작해`, `5%로 작업 시작해`를 같은 규칙으로 처리한다.
- `N%로 작업 시작해`로 시작한 하나의 작업 묶음은 시작할 때 확인한 iOS 커밋을 고정 기준으로 사용한다. 작업 중 iOS가 변경되어도 다시 읽어 기준을 바꾸지 않으며, 허용된 작업 묶음을 모두 마친 뒤 다음 작업을 시작할 때 변경된 iOS를 한 번에 비교한다.
- CCMB가 표시하는 현재 주간 Codex 사용률을 시작값으로 기록하고, 시작값보다 `N`%포인트 증가할 때까지 작업한다.
- 사용량은 의미 있는 작업 묶음 사이에 CCMB와 같은 기준으로 다시 확인한다. `N`%포인트에 도달하면 새 작업을 시작하지 않고 즉시 마무리한다.
- 중간 진행률 보고에는 간헐적으로 `허용 N%, 사용 U%` 형식만 짧게 표시한다.
- 진행 중 사용자가 사용량을 추가로 허용하면 기존 허용량에 퍼센트포인트로 합산한다. 작업별 총 한도는 해당 작업에서 사용자가 마지막으로 허용한 누적 퍼센트포인트를 따른다.
- 주간 사용량 반영에는 지연과 정수 단위 반올림이 있을 수 있으므로 정확히 `N.00`%포인트에서 멈추지 못할 가능성을 완료 보고에 밝힌다.
- 작업의 첫째 목표는 최신 iOS 코드의 기능, 동작 수치, 데이터 구조를 파악해 Android에 가능한 한 동일하게 구현하는 것이다. 둘째 목표는 기능 동등성을 해치지 않는 범위에서 화면 디자인을 가능한 한 동일하게 맞추는 것이다.
- 작업이 여러 날 이어지면 매일 첫 Android 작업 전에 iOS 기준 커밋과 전날 이후 변경 파일을 확인하고 대응 작업에 반영한다.
- iOS 동등성은 기능, 동작 수치, 데이터 구조, 디자인의 검증 가능한 체크리스트로 계산하며, 확인하지 못한 항목은 달성한 것으로 계산하지 않는다.
- 마무리할 때 주간 사용량 시작값과 종료값, 실제 증가분, iOS 동등성 달성률을 보고한다.
- 마무리할 때 iOS와 더 같아지기 위해 필요한 잔여 작업을 기능 동등성과 디자인 동등성으로 나눠 예상 소요 날짜와 추가 주간 사용량도 함께 보고한다.
